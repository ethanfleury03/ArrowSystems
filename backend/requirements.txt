# ============================================================================
# CPU-Only Requirements for Cloud Run Deployment
# ============================================================================
# This file is optimized for small Docker images (<400MB)
# All packages are CPU-only versions to avoid GPU wheel bloat
# Models download at runtime in /app/.cache, NOT during pip install
# ============================================================================

# -----------------------------
# PyTorch (CPU-Only) - MUST BE FIRST
# -----------------------------
# CPU-only PyTorch (~200MB instead of 4-6GB GPU version)
# Install from PyTorch CPU wheel index to ensure CPU-only build
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0+cpu

# -----------------------------
# Core ML and AI (CPU-Only, No Auto-Download)
# -----------------------------
# Pinned to stable versions that do NOT download models during pip install
# Models are downloaded at runtime in /app/.cache/huggingface
transformers==4.40.0  # Stable version, no auto-download, CPU-compatible, works with sentence-transformers 2.6.1
sentence-transformers==2.6.1  # Minimum version required by llama-index-embeddings-huggingface, CPU-compatible
huggingface-hub==0.22.2  # Stable version, no auto-download
safetensors==0.6.2
accelerate==0.30.1  # CPU-only version, compatible with transformers 4.40.0
scikit-learn==1.7.2
numpy==1.26.4

# -----------------------------
# LlamaIndex
# -----------------------------
# LlamaIndex works with CPU-only PyTorch and transformers
# IMPORTANT: Install only specific packages we use, NOT the meta-package
# The meta-package pulls in many optional deps with loose constraints causing resolution loops
llama-index-core==0.14.5
llama-index-embeddings-huggingface==0.6.1
llama-index-vector-stores-qdrant==0.8.6  # Optional: Only needed if USE_QDRANT=true
qdrant-client==1.12.2  # Optional: Only needed if USE_QDRANT=true

# -----------------------------
# Google Cloud Storage
# -----------------------------
google-cloud-storage>=2.10.0

# -----------------------------
# API and LLM
# -----------------------------
anthropic==0.71.0

# -----------------------------
# Database (PostgreSQL via SQLAlchemy)
# -----------------------------
bcrypt==4.2.0
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9

# -----------------------------
# FastAPI Backend
# -----------------------------
fastapi==0.120.0
python-multipart
uvicorn[standard]==0.38.0
gunicorn==21.2.0
pydantic==2.12.3
email-validator==2.2.0
slowapi==0.1.8

# -----------------------------
# Document Processing
# -----------------------------
PyMuPDF==1.26.5
pandas==2.2.0
python-docx==1.2.0
python-dotenv==1.1.1
Pillow==11.3.0
PyJWT==2.9.0
# Ensure old 'jwt' package is not installed (conflicts with PyJWT)
# PyJWT provides the 'jwt' module, so we don't need the old 'jwt' package

# -----------------------------
# Utilities
# -----------------------------
tqdm==4.67.1
rank-bm25==0.2.2
pyyaml==6.0.3
structlog==24.1.0

# -----------------------------
# Core Dependencies
# -----------------------------
requests==2.32.5
urllib3==2.5.0
certifi==2025.10.5
typing-extensions==4.15.0
packaging==25.0
setuptools==80.9.0
filelock==3.20.0
fsspec==2025.9.0
networkx==3.4.2
jinja2==3.1.6
click==8.3.0
h11==0.16.0
anyio==4.11.0
starlette==0.48.0
annotated-types==0.7.0
pydantic-core==2.41.4
